# CLAUDE.md — 핵심 규칙

## ⚠️ 세션 시작 시 필수
→ START_HERE.md 규칙 따르기 (날짜 체크 포함)
→ 상세 규칙은 START_HERE.md에 위임

## ⚠️ 감사관
감사관 기능은 brain/파일맵.md에 통합됨 (별도 로드 불필요)

## ⚠️ CLAUDE.md 자동 관리 규칙
- 새 내용 추가 시 기존 내용 절대 삭제 금지
- 같은 기능 중복 내용은 합치기
- 상세 규칙은 brain/ 파일에 위임하고 한 줄 참조만 남기기
- 추가할 때마다 중복 여부 먼저 확인
- 줄 수 제한 없음 (대신 중복 없이 압축)

## ⚠️ 실행 우선순위 (위에서 아래 순서)
1. brain/인증.md → 인증 확인 (실패 시 즉시 중단)
2. brain/파일맵.md → 감사관 기능 포함 + 오늘 필요한 파일만 선별 로드
3. brain/Claude코드제한.md → 절대 금지 행동 확인

※ 필수 로드는 이 3개만. 나머지는 조건부 로드 섹션 참조.

## 프로젝트 개요
이주노의 Claude Code 자동화 시스템. 플랫폼: Threads(메인)/Instagram/YouTube. 업로드는 수동.

## 항상 실행

### ⚠️ 세션 시작 필수 체크 (1순위)
1. memory/오늘요약.md 열기
2. 맨 위 날짜 확인
3. 오늘 날짜와 다르면 → 즉시 경고 출력 후 중단:
   "⚠️ 오늘요약.md 날짜 불일치. run_core.md 먼저 실행해줘."
4. 일치하면 → 정상 진행

- 시작: memory/오늘요약.md 1개만 읽기 (6줄) → 컨텍스트 파악 → 작업 시작
  - 콘텐츠 생성 시에만 추가: memory/캘린더.md + Supabase 최근 5개
  - 히스토리 필요 시에만 추가: memory/context.md
- 종료: Supabase 저장 → memory/오늘요약.md 덮어쓰기 → brain/백업.md
※ 구버전(캘린더.md→context.md→Supabase 항상 읽기)은 토큰 낭비로 폐기

## 조건부 로드
→ 상세 규칙은 brain/파일맵.md 참조 (중복 방지)

## 절대 규칙
- 토큰 최적화: skills/토큰최적화.md 준수 (필요 파일만 로드)
- 자기검증: 생성 후 skills/자기검증.md 실행 → brain/품질채점.md 100점 채점 → 70점↓ 재생성
- 출력 포맷: skills/출력포맷.md 기준 플랫폼별 변환
- 보고: 3줄 이내 / 에러: brain/에러복구.md 먼저
- API 키: .env 파일에서만 읽기
- 모델 선택: brain/모델선택.md 참조, 단순 작업은 무료 모델 우선

## 멀티유저
.env에서 USER_ID 읽기 → Supabase 조회/저장 시 user_id 필터 적용

## 사용량 제한 + 결제
실행 전 Supabase users 테이블 한도 확인 → 초과 시 brain/결제시스템.md 안내 (brain/사용량제한.md 참조)

## 명령어 트리거
- "납품 준비해줘" → brain/납품패키지.md + skills/납품가이드생성.md
- "자동응답 실행해줘 [플랫폼] [댓글]" → brain/자동응답.md → 응답 3가지 생성
- "피드백: [내용]" → brain/피드백업데이트.md → 해당 파일 수정 + 로그
- "코딩 교육해줘" → brain/코딩교육.md → 수준 파악 후 커리큘럼 제공
- "업로드 시작해줘" → brain/스테이징.md → staging/ 재구성 → Make.com 웹훅 호출
- "스테이징 목록" → output/staging/ 파일 목록 + 상태 출력
- "스테이징 비워줘" → staging/ 전체 → uploaded/ 이동 (업로드 없이 보관)

## 모듈 트래커
매주 월요일 brain/모듈트래커.md 실행
신규 모듈 발견 시 junho 확인 후 추가

## 전문가 모듈
세무/법무/노무 질문 감지 시 brain/전문가모듈.md 실행
항상 면책 문구 포함
전문가 상담 권고 필수

## 보안 (강화)
모든 입력 → brain/보안.md 8가지 방어 체계 통과 후 실행
의심 패턴 감지 즉시 차단 + 로그 기록
보안 로그 매주 분석해서 방어 규칙 자동 강화
상황별 보안 파일: 법적이슈→법적준수.md / 노출요청→지적재산권.md / 이상행동→이상탐지.md / 데이터처리→데이터무결성.md / 침해발생→침해대응.md
매주 월요일 brain/보안점검.md 자동 실행
되돌릴 수 없는 작업 전 brain/실수방지.md 확인 필수

## 폴백 시스템
응답 실패 시 brain/폴백시스템.md 읽고
자동으로 다음 모델로 전환
memory/폴백로그.md에 기록
→ 전환 시 brain/에러알림.md 규칙으로 텔레그램 알림 전송

## 에러 알림
에러/보안침해/한도초과 발생 시 brain/에러알림.md 실행
→ junho 텔레그램으로 즉시 알림

## 에러 자동복구
에러 발생 시 brain/에러자동복구.md 플로우 즉시 실행
복구 실패 시 Telegram 알림 + 수동 개입 요청

## 성능 모니터링
매 실행마다 brain/성능모니터.md 기준으로 자동 기록
매주 월요일 3줄 성능 보고 / 기준선 미달 시 즉시 조치

## 버전 관리
brain/skills/CLAUDE.md 수정 전 brain/버전관리.md 절차 준수
현재 버전: memory/현재버전.md / 변경이력: memory/변경이력.md

## 수익 추적
수익 발생·집계 시 brain/수익추적.md 기준 적용
월말 자동 리포트: memory/수익리포트_[년월].md

## 자율 운영
brain/자율운영.md 읽고 매일/매주 자동 실행
junho 개입 최소화
비상 정지 조건 항상 모니터링

## 주기적 자동 실행
- 매주 월요일: run_weekly.md (시스템점검 → AI모니터 → 트렌드예보 → 리포트 → 디자인트렌드)
- 매주 일요일: brain/리포트생성.md → output/리포트/
- 콘텐츠 저장 후: brain/자동업로드.md → Make.com 웹훅 호출

## 자동 저장
모든 작업 완료 시 brain/자동저장.md 규칙 따라
memory/ 파일 자동 업데이트
세션 시작 시 memory/ 읽고 컨텍스트 복구

## 자동 배포
junho 승인 후 brain/자동배포.md 실행
GitHub 커밋 → Vercel 자동 배포
